
  # High-fidelity wireframe prompt

  This is a code bundle for High-fidelity wireframe prompt. The original project is available at https://www.figma.com/design/uZfuH92mLUTpMTCQD5Rpe3/High-fidelity-wireframe-prompt.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  